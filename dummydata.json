db.collection('todos').insertMany([
    // MongoDB adds the _id field with an ObjectId if _id is not present
    {
        test: "Task One",
        body: "Do someStruff"
    },
    {
        item: "Milk?",
        body: "GET THAT MILK!"
    }
])